# بيانات الهجرة تُعامَل بايت-ببايت: أي تحويل لنهايات الأسطر يكسر بصمة SHA-256 المعتمدة.
docs/migrations/*.json -text
# وكذلك سكربتات SQL: البصمة التي يوافق عليها المالك هي بصمة البايتات التي ستُنفَّذ،
# فتحويل CRLF عند السحب كان سيجعل الملف المُراجَع والملف المُنفَّذ مختلفَين.
docs/migrations/*.sql -text
docs/security/*.sql -text
# بيان الافتتاح: بصمته بصمة بايتاته — لا تحويل نهايات أسطر.
docs/accounting/*.json -text
