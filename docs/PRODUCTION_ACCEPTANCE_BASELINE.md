# UME-PMS-V2-PRODUCTION-ACCEPTANCE-BASELINE

**تاريخ التجميد:** 2026-08-12
**الحالة:** مُجمَّدة ومقبولة للتشغيل

هذه الوثيقة تُثبّت النسخة المقبولة من UME PMS v2 بعد سلسلة الإصلاحات
`R1 → R2 → R2.6 → R3A → R3A.1 → R3A.2 → R3B → R3C`، وتفصل رسمياً بين مرحلة
**التثبيت والمعالجة** ومرحلة **تطوير المنتج المستقبلي**.

---

## 1 · النسخة المقبولة

الـBaseline **يتكوّن من مستودعين معاً** — لا يكفي أحدهما لتحديدها:

| | المستودع | الالتزام | النشر |
|---|---|---|---|
| **الباك** | `ume-pms-v2` | `c6e5b6e2` | Railway · `ume-pms-v2-backend-production.up.railway.app` |
| **الفرونت** | `ume-frontend` | `932a53d` | Vercel · `ume-pms-v2-frontend.vercel.app` |

**الوسم في كلا المستودعين:** `UME-PMS-V2-PRODUCTION-ACCEPTANCE-BASELINE`

قاعدة البيانات: Supabase Postgres عبر **Session Pooler · المنفذ 5432**.

---

## 2 · الاختبارات والبناء

```
Tests           : 292 / 292  (14 مجموعة · صفر إخفاق · صفر متخطّى)
Type-check      : PASS  (الباك والفرونت)
Backend build   : PASS
Frontend build  : PASS
```

---

## 3 · لقطة الإنتاج المالية

**قاعدة حاكمة: لا يوجد أي إجمالي يجمع عملات مختلفة. كل عملة دفتر مستقل.**

### الفواتير حسب الحالة

| الحالة | العدد |
|---|---|
| مدفوعة | 146 |
| غير مدفوعة | 84 |
| **الإجمالي** | **230** |

### الفواتير حسب العملة

| العملة | العدد | الإجمالي | المسدَّد | المتبقّي |
|---|---|---|---|---|
| USD | 140 | 5,075,032.51 | 3,782,184.94 | 1,292,847.57 |
| EUR | 85 | 329,982.61 | 210,259.91 | 119,722.70 |
| SAR | 4 | 24,679.59 | 23,789.59 | 890.00 |
| CHF | 1 | 25,384.94 | 25,384.94 | 0.00 |

### السدادات حسب العملة

| العملة | العدد | الإجمالي |
|---|---|---|
| USD | 11 | 501,821.17 |
| EUR | 4 | 26,431.87 |
| CHF | 1 | 25,384.94 |
| SAR | 2 | 19,869.70 |
| **الإجمالي** | **18** | — |

### التصنيف التاريخي (R3A)

| المبرّر | العدد |
|---|---|
| `pre_system_settled` | 123 |
| `credit_note` | 5 |
| **دفعة `LEGACY-2026-08`** | **128** |

بيان العضوية المجمَّد: [`docs/migrations/r3a-legacy-2026-08-manifest.json`](migrations/r3a-legacy-2026-08-manifest.json)
بصمة السجلات: `4c95613ada56bea0c953a15e109c29873c3d23021276771c8933c16ac6cf4901`

### محرّك التدقيق

```
حرِج 0 · مرتفع 0 · متوسط 0 · منخفض 9 · معلوماتي 128
التعرُّض المالي        : {}   — صفر بكل عملة
تعرُّض التصنيف التاريخي : {}   — صفر بكل عملة
```

التسع المنخفضة مؤشرات رقابية لا أخطاء: خمسة موردين متعددي العملات · أربع فواتير بلا مركب.

**للمقارنة — نقطة البداية في تدقيق R1:** 252 حالة حرجة · USD 3,345,813.27 + EUR 190,928.04 + SAR 3,919.89.

### أعداد الكيانات

```
الموردون 74 · المراكب 7 · أوامر الشراء 44 · العملاء 3
فواتير الإيجار 12 · الفواتير الإدارية 17 · المهام 3 · البنود 21 · المستخدمون 5
```

---

## 4 · ضوابط القبول المالي

| الضابط | النتيجة | الدليل |
|---|---|---|
| السلامة التشغيلية | **PASS** | 102 فاتورة تشغيلية · **صفر تباين غير متوقَّع** |
| سلامة السداد | **PASS** | ثلاثة مسارات فقط تكتب حالة السداد، كلها إعادة حساب من سجلات الدفع داخل معاملات |
| نزاهة العملات | **PASS** | خمسة موردين متعددي العملات — دفاتر مستقلة والإجماليات الموحّدة `null` |
| كشف حساب المورد | **PASS** | دفتر لكل عملة · بلا إجمالي جامع |
| تجميعات الموردين | **PASS** | مفصولة بالعملة · حقول `paidViaPms` / `settledPreSystem` / `unevidencedResidual` |
| تجميعات المراكب | **PASS** | EUR · SAR · USD منفصلة · بلا إجمالي جامع |
| التصنيف التاريخي | **PASS** | 128 = 123 + 5 · تعرُّض صفر · محميّ من إعادة الاشتقاق |
| محرّك التدقيق | **PASS** | يفصل التشغيلي عن التاريخي · صفر حرِج |

**متطابقة التسوية:** `paid_amount = paidViaPms + settledPreSystem + creditNoteOffset + unevidencedResidual`
`unevidencedResidual` = **صفر** عبر كل الموردين والمراكب.

---

## 5 · ضوابط الأمان

| الضابط | النتيجة |
|---|---|
| التفويض خادمي | **PASS** — `JwtAuthGuard` + `ScreenGuard` على المسارات |
| صلاحيات صريحة لغير الأدمن | **PASS** — 3/3 (16 · 16 · 14 شاشة) |
| المنع بالافتراض | **PASS** — `allowed_screens = null` لغير الأدمن ⇒ رفض |
| بلا توكن | **PASS** — 401 |
| الدور الافتراضي | `user` |
| حارس admin → user | **PASS** — يستلزم `allowed_screens` صريحة |
| أدوار مجهولة | **صفر** |
| `synchronize` في الإنتاج | **false** — صريح · fail-closed · محروس بـassertion |
| مُشغِّل R3A | **DISABLED** — 404 بجلسة أدمن |

### الأسرار — كلها مُدوَّرة

| السر | الحالة |
|---|---|
| `ANTHROPIC_API_KEY` | ✅ مُدوَّر |
| `JWT_SECRET` | ✅ مُدوَّر — الرموز القديمة مُبطَلة بتغيير التوقيع |
| `DATABASE_URL` | ✅ مُدوَّر — القديمة مُبطَلة بآلية Reset في Supabase |
| `SUPABASE_SERVICE_KEY` | ✅ مُدوَّر — مقبول تشغيلياً |

**لا تُخزَّن أي قيمة سرّية في هذا المستودع ولا في هذه الوثيقة.** بيانات الاعتماد التاريخية
الموجودة في تاريخ Git **مُبطَلة** بعد التدوير — ولم تُعَد كتابة التاريخ عمداً.

---

## 6 · سلامة المخطط

```
synchronize في الإنتاج : false        migrationsRun : false
تغييرات المخطط         : بهجرة صريحة فقط
انحراف المخطط          : صفر — القيود الأربعة قائمة عبر النشرات
```

القيود الحيّة على `invoices`: `chk_inv_data_origin` · `chk_inv_settlement_basis` ·
`chk_inv_presystem_requires_batch` · `fk_invoices_import_batch`.

السياسة الكاملة: [`docs/SCHEMA_CHANGE_POLICY.md`](SCHEMA_CHANGE_POLICY.md)

---

## 7 · المساعدون الذكيون

| المساعد | الحالة |
|---|---|
| Ask UME | ✅ يعمل |
| Fleet Assistant | ✅ يعمل |
| Tasks Assistant | ✅ يعمل |
| Invoices Assistant | ✅ يعمل |

**السلوك الآمن مُختبَر:** عند السؤال عن فاتورة غير موجودة يجيب بأنه **لم يعثر عليها**
ولا يخترع رقماً. ويُمنع منعاً باتاً من ذكر تاريخ سداد لفاتورة بلا سجل دفع.

---

## 8 · الدَّين المقبول — غير مانع

| ID | البند | الحالة |
|---|---|---|
| **A1** | رفع مرفق بمفتاح Supabase الجديد لم يُثبَت بكتابة إنتاج | يُثبَت بأول رفع طبيعي |
| **A2** | إبطال مفتاح Supabase القديم | `UNKNOWN` — يُحسم من لوحة Supabase |
| **A3** | مفتاح Anthropic القديم `UME PMS v2` | يُنصح بحذفه من الكونسول |
| **S2** | `ssl.rejectUnauthorized: false` | دَيْن محكوم — يُنظر فيه لاحقاً |
| **T1** | لا بنية هجرات كاملة | دَيْن محكوم — السياسة موثَّقة |
| **T2** | مُشغِّل R3A باقٍ في الشيفرة | **قدرة تعافٍ خاملة محكومة** — بوابتان فعّالتان |
| **C1** | `net_outstanding` في الإيجار بلا فحص عملة | حارس وقائي · **صفر حالة** |
| **C2** | `paymentsSum` في كشف المورد بلا فحص عملة | حارس وقائي · **صفر حالة** |
| **O2** | 4 فواتير بلا مركب | العمود nullable بالتصميم |

**لا يوجد بند من فئة CRITICAL. لا يوجد بند يمنع القبول.**

---

## 9 · مرجع التراجع

- **الباك:** أي التزام سابق لـ`c6e5b6e2` — ⚠️ **تحذير:** الإرجاع لنسخة لا تعرف أعمدة R3A
  كان سيُسقِطها سابقاً؛ هذا الخطر **مُغلَق** الآن بـ`synchronize: false`.
- **الفرونت:** أي التزام سابق لـ`932a53d`.
- **بيانات R3A:** قابلة لإعادة الإنتاج بالكامل من البيان المجمَّد عبر مُشغِّل R3A
  (يتطلب `R3A_RUNNER_ENABLED=true` مؤقتاً).
- **لقطة ما قبل الهجرة:** [`docs/migrations/r3a-pre-migration-snapshot.csv`](migrations/r3a-pre-migration-snapshot.csv)

---

## 10 · حدّ هذه الوثيقة

هذه لقطة **بتاريخ التجميد**. أي نشاط إنتاج مشروع بعدها (فواتير أو سدادات جديدة)
سيغيّر الأعداد — وهذا **متوقَّع ولا يُعدّ انحرافاً**. الثابت الذي يجب أن يبقى صحيحاً:

- تباينات تشغيلية غير متوقَّعة = **صفر**
- تعرُّض التصنيف التاريخي = **صفر**
- حالة السداد مشتقّة من سجلات الدفع **حصراً**
- لا يوجد أي إجمالي يجمع عملات مختلفة بلا سعر صرف موثَّق
